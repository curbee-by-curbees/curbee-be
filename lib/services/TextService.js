import { sendSms } from '../lib/utils/twilio.js';

// what should the text service do?
// twilio instead of front end?
// how do we use twilio to receive data we can add to the database and give back to users?
// can we get a photo from a text?
// can we send a photo in a text?
// how do we initiate our service from the text msg that's coming in?
// 
